name: Deploy
run-name: Do Nothing
on:
  push:
    branches:
      - master
jobs:
  No-Op:
    name: Do Nothing
    runs-on: self-hosted
    environment: prod
    steps:
      - name: Clone repo
        uses: actions/checkout@v4

